# NuaSpa mikroservisna arhitektura:
# - nuaspa-db      → SQL Server (baza 210240)
# - rabbitmq       → message broker (API ↔ Worker)
# - nuaspa-api     → glavni REST servis (Flutter klijent)
# - nuaspa-worker  → pomoćni servis (RabbitMQ consumer, e-mail)
#
# Prije pokretanja: cp .env.docker.example .env  (prilagodi vrijednosti)
# Pokretanje: docker compose up --build
# API:        http://localhost:8080/health
# RabbitMQ UI: http://localhost:15672

services:
  nuaspa-db:
    image: mcr.microsoft.com/mssql/server:2022-CU14-ubuntu-22.04
    container_name: nuaspa-db-container
    hostname: nuaspa-db
    env_file:
      - .env
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: ${MSSQL_SA_PASSWORD}
      MSSQL_PID: Developer
    ports:
      - "1433:1433"
    volumes:
      - nuaspa-sql-data:/var/opt/mssql
    networks:
      - nuaspa-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"$$MSSQL_SA_PASSWORD\" -C -Q 'SELECT 1' || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 40s
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:3.13.7-management
    container_name: rabbitmq-nuaspa
    hostname: rabbitmq
    env_file:
      - .env
    environment:
      RABBITMQ_DEFAULT_USER: ${RabbitMQ__UserName}
      RABBITMQ_DEFAULT_PASS: ${RabbitMQ__Password}
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    networks:
      - nuaspa-network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 8
      start_period: 25s
    restart: unless-stopped

  nuaspa-api:
    image: nuaspa-api:1.0.0
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nuaspa-api-container
    hostname: nuaspa-api
    env_file:
      - .env
    environment:
      ASPNETCORE_ENVIRONMENT: Docker
      ASPNETCORE_URLS: http://+:8080
      UseHttpsRedirection: "false"
      ConnectionStrings__DefaultConnection: Server=nuaspa-db;Database=210240;User Id=sa;Password=${MSSQL_SA_PASSWORD};TrustServerCertificate=True;MultipleActiveResultSets=true
      RabbitMQ__Host: rabbitmq
    ports:
      - "8080:8080"
    depends_on:
      nuaspa-db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - nuaspa-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 15s
      timeout: 5s
      retries: 6
      start_period: 60s
    restart: unless-stopped

  nuaspa-worker:
    image: nuaspa-worker:1.0.0
    build:
      context: .
      dockerfile: Dockerfile.worker
    container_name: nuaspa-worker-container
    hostname: nuaspa-worker
    env_file:
      - .env
    environment:
      DOTNET_ENVIRONMENT: Docker
      RabbitMQ__Host: rabbitmq
      Email__OutboxDirectory: /app/email-outbox
    volumes:
      - worker-email-outbox:/app/email-outbox
    depends_on:
      rabbitmq:
        condition: service_healthy
      nuaspa-api:
        condition: service_started
    networks:
      - nuaspa-network
    restart: unless-stopped

volumes:
  nuaspa-sql-data:
  rabbitmq-data:
  worker-email-outbox:

networks:
  nuaspa-network:
    name: nuaspa-network
    driver: bridge
